<UserControl x:Class="SysCleaner.Wpf.Views.WindowsUpdateRepairView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardBorderStyle}" Background="{StaticResource Brush.SurfaceMuted}">
            <StackPanel>
                <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="系统更新" />
                <TextBlock Style="{StaticResource PageSubtitleTextStyle}" Text="单独处理 Windows Update 服务、更新缓存和组件修复，不等同于直接安装系统补丁。" />
                <WrapPanel Margin="0,18,0,0">
                    <Button Content="刷新诊断" Command="{Binding RefreshCommand}" />
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="重启更新服务" Command="{Binding RestartCoreServicesCommand}" />
                    <Button Style="{StaticResource WarningButtonStyle}" Content="重置更新组件" Command="{Binding ResetWindowsUpdateComponentsCommand}" />
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="运行 DISM" Command="{Binding RunDismRestoreHealthCommand}" />
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="运行 SFC" Command="{Binding RunSfcScanCommand}" />
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="卸载所选 KB" Command="{Binding UninstallSelectedUpdateCommand}" />
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="复制失败详情" Command="{Binding CopyFailureDetailCommand}" />
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="清除错误筛选" Command="{Binding ClearFailureFilterCommand}" />
                </WrapPanel>
                <Border Style="{StaticResource CardBorderStyle}" Margin="0,10,0,0" Padding="16" Background="White">
                    <TextBlock Text="此页面用于修复 Windows Update 环境本身，例如服务异常、缓存堆积、组件存储损坏。是否安装补丁仍应由系统更新流程决定。" TextWrapping="Wrap" Foreground="{StaticResource Brush.SecondaryText}" />
                </Border>
                <WrapPanel Margin="0,12,0,0">
                    <Border Style="{StaticResource ChipBorderStyle}">
                        <TextBlock Text="{Binding PendingRebootText}" />
                    </Border>
                    <Border Style="{StaticResource ChipBorderStyle}">
                        <TextBlock Text="{Binding LastSuccessfulInstallText}" />
                    </Border>
                </WrapPanel>
            </StackPanel>
        </Border>

        <ItemsControl Grid.Row="1" Margin="0,18,0,0" ItemsSource="{Binding Items}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="300"
                            Margin="0,0,16,16"
                            Padding="18"
                            Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CornerRadius="20"
                            Background="White"
                            BorderBrush="{StaticResource Brush.Border}"
                            BorderThickness="1">
                            <Border.ContextMenu>
                                <ContextMenu DataContext="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                                    <MenuItem Header="执行推荐操作" Command="{Binding RunDiagnosticActionCommand}" CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <MenuItem Header="复制诊断信息" Command="{Binding CopyDiagnosticCommand}" CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    <Separator />
                                    <MenuItem Header="刷新诊断" Command="{Binding RefreshCommand}" />
                                </ContextMenu>
                            </Border.ContextMenu>
                            <StackPanel>
                            <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource Brush.PrimaryText}" />
                            <TextBlock Margin="0,8,0,0" Text="{Binding Description}" TextWrapping="Wrap" Foreground="{StaticResource Brush.SecondaryText}" />
                            <TextBlock Margin="0,12,0,0" Text="{Binding DetectionSummary}" TextWrapping="Wrap" Foreground="{StaticResource Brush.PrimaryText}" />
                            <TextBlock Margin="0,8,0,0" Text="{Binding Recommendation}" TextWrapping="Wrap" Foreground="{StaticResource Brush.SecondaryText}" />
                            <Border Margin="0,12,0,0" Style="{StaticResource ChipBorderStyle}" HorizontalAlignment="Left">
                                <TextBlock Text="{Binding Health}" />
                            </Border>
                        </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
        </ItemsControl>

        <WrapPanel Grid.Row="2" Margin="0,2,0,0" ItemWidth="460">
            <Border Width="460" Margin="0,0,18,18" Style="{StaticResource CardBorderStyle}" Padding="20,16,20,20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="已安装 KB" />
                        <TextBlock Margin="0,6,0,0" Text="展示最近检测到的已安装补丁记录，并标记安全更新或功能更新类别。" TextWrapping="Wrap" Foreground="{StaticResource Brush.SecondaryText}" />
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding InstalledUpdates}" SelectedItem="{Binding SelectedInstalledUpdate}">
                        <DataGrid.ContextMenu>
                            <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="刷新诊断" Command="{Binding RefreshCommand}" />
                                <Separator />
                                <MenuItem Header="卸载当前 KB" Command="{Binding UninstallSelectedUpdateCommand}" />
                                <MenuItem Header="复制 KB 信息" Command="{Binding CopyInstalledUpdateCommand}" />
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="KB" Binding="{Binding KbId}" Width="*" />
                            <DataGridTextColumn Header="说明" Binding="{Binding Title}" Width="2*" />
                            <DataGridTextColumn Header="类别" Binding="{Binding UpdateType}" Width="*" />
                            <DataGridCheckBoxColumn Header="安全更新" Binding="{Binding IsSecurityUpdate}" Width="*" />
                            <DataGridTextColumn Header="安装时间" Binding="{Binding InstalledOn}" Width="*" />
                            <DataGridTextColumn Header="安装者" Binding="{Binding InstalledBy}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <Border Width="460" Margin="0,0,18,18" Style="{StaticResource CardBorderStyle}" Padding="20,16,20,20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="错误码聚合" />
                        <TextBlock Margin="0,6,0,0" Text="相同错误码按次数聚合，便于快速判断主要失败模式。" TextWrapping="Wrap" Foreground="{StaticResource Brush.SecondaryText}" />
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding FailureGroups}" SelectedItem="{Binding SelectedFailureGroup}">
                        <DataGrid.ContextMenu>
                            <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="复制错误码摘要" Command="{Binding CopyFailureGroupCommand}" />
                                <MenuItem Header="清除此错误筛选" Command="{Binding ClearFailureFilterCommand}" />
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="错误码" Binding="{Binding ErrorCode}" Width="*" />
                            <DataGridTextColumn Header="次数" Binding="{Binding Count}" Width="*" />
                            <DataGridTextColumn Header="最近时间" Binding="{Binding LatestTime}" Width="*" />
                            <DataGridTextColumn Header="最近标题" Binding="{Binding LatestTitle}" Width="2*" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock Grid.Row="2" Margin="0,16,0,0" Style="{StaticResource SectionTitleTextStyle}" Text="最近失败记录" />
                    <TextBlock Grid.Row="2" Margin="0,50,0,0" Text="{Binding FailureFilterText}" Foreground="{StaticResource Brush.SecondaryText}" />
                    <DataGrid Grid.Row="3" ItemsSource="{Binding RecentFailures}" SelectedItem="{Binding SelectedFailure}">
                        <DataGrid.ContextMenu>
                            <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="复制失败详情" Command="{Binding CopyFailureDetailCommand}" />
                                <MenuItem Header="刷新诊断" Command="{Binding RefreshCommand}" />
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="时间" Binding="{Binding Timestamp}" Width="*" />
                            <DataGridTextColumn Header="结果" Binding="{Binding Result}" Width="*" />
                            <DataGridTextColumn Header="标题" Binding="{Binding Title}" Width="2*" />
                            <DataGridTextColumn Header="错误码" Binding="{Binding ErrorCode}" Width="*" />
                            <DataGridTextColumn Header="详情" Binding="{Binding Details}" Width="2*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </WrapPanel>
    </Grid>
</UserControl>