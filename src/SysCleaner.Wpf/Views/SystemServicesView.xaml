<UserControl x:Class="SysCleaner.Wpf.Views.SystemServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:behaviors="clr-namespace:SysCleaner.Wpf.Behaviors">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardBorderStyle}" Background="{StaticResource Brush.SurfaceMuted}">
            <WrapPanel ItemWidth="520" Orientation="Horizontal">
                <StackPanel Width="520" Margin="0,0,18,12">
                    <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="系统服务" />
                    <TextBlock Style="{StaticResource PageSubtitleTextStyle}" Text="扫描第三方服务与残留服务注册项，并支持停止后卸载服务。" />
                    <Border Style="{StaticResource CardBorderStyle}" Margin="0,12,0,0" Padding="16" Background="White">
                        <TextBlock Text="默认仅展示第三方服务、残留服务和与已安装软件有关联的服务。系统目录下的微软服务会被保护，不提供卸载入口。" TextWrapping="Wrap" Foreground="{StaticResource Brush.SecondaryText}" />
                    </Border>
                </StackPanel>
                <WrapPanel VerticalAlignment="Top">
                    <Button Content="刷新" Command="{Binding RefreshCommand}" />
                    <Button Style="{StaticResource DangerButtonStyle}" Content="卸载服务" Command="{Binding DeleteCommand}" />
                </WrapPanel>
            </WrapPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="0,18,0,0" Padding="20,16,20,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel>
                    <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="服务候选项" />
                    <TextBlock Margin="0,6,0,0" Text="卸载服务前建议先确认可执行文件路径、显示名称和来源描述。" TextWrapping="Wrap" Foreground="{StaticResource Brush.SecondaryText}" />
                </StackPanel>
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Items}"
                          SelectedItem="{Binding SelectedItem}"
                          behaviors:DataGridRowSelectionBehavior.DoubleClickCommand="{Binding DeleteCommand}">
                    <DataGrid.ContextMenu>
                        <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                            <MenuItem Header="刷新列表" Command="{Binding RefreshCommand}" />
                            <Separator />
                            <MenuItem Header="卸载当前服务" Command="{Binding DeleteCommand}" />
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="显示名称" Binding="{Binding Title}" Width="2*" />
                        <DataGridTextColumn Header="服务信息" Binding="{Binding Source}" Width="2*" />
                        <DataGridTextColumn Header="目标文件" Binding="{Binding TargetPath}" Width="2*" />
                        <DataGridTextColumn Header="状态" Binding="{Binding Health}" Width="*" />
                        <DataGridTextColumn Header="建议" Binding="{Binding Evidence}" Width="2*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>